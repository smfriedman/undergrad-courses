package hw4;
import lab7.AbstractNode;

/** The short-circuiting or operator (||).  If the left child is true, then
  * the right child should not execute.
  */
public interface ShortOrIsh extends ASTNodeIsh {
}
